I want AS IDENTICAL OF AN architecture to what we already have as possible. But I want cleanup of inputs and outputs at each layer. NO DRIFTING BETWEEN VARIABLES CREATED WITHIN FUNCTIONS, NOR DRIFTING FROM VARIABLE NAMES ACCEPTED BY DOWNSTREAM FUNCTIONS. I want the code, itself, to stay as identical to what we already have as possible-- the style and docstrings, FOR SURE, and the functions provided that they remain necessary.

I am checking through each file as I write this.

I want to keep the internal data processing step and revise those functions, "load_bait_data" and "extract_bait_matrix".
As before, I want the required argument, "data", to be passed by the user to the pipeline FOR BOTH pipelines and to be in wide format. The "load_bait_data" and "extract_bait_matrix" functions inside io/data_input.py still receive either a pandas dataframe or a .csv file in wide format and transform it to long. The only changes to this file are naming conventions: "extract_bait_matrix"'s argument is "df". "extract_bait_matrix" is called inside "load_bait_data", which first takes the argument, "input_obj" (which I infer is "data" passed by the user to the pipeline), and unifies the .csv or pandas dataframe into a pandas dataframe, "df". Let's rename this object more explicitly to "user_provided_df", and also change "extract_bait_matrix"'s argument from "df" to "user_provided_df".

"load_bait_data"'s outputs, "bait_list", "X_by_bait", and "metadata" are fine.


What The User Passes: required: "data" in wide format, either as a pandas dataframe or as a .csv.


In a previous iteration required a second argument, subsequently made an optional argument, "metadata". This is a pandas dictionary so structured: { "biological_bait_names" : {"TMEMV5" : "TMEM184B", "TMEMmyc" : "TMEM184B", "BirAV5": "birA", "GFPmyc": "GFP"}, "AN" : {"TMEM184B" : "Q9Y519"}, "MW" : {"TMEM184B" : 46 } }. This metadata is preserved and included as an object in the pipeline output. For the purpose of iterating the em engine over baits, this may be a useful, required object

As before, the optional argument, "metadata", is preserved for the pipeline output, and users optionally pass (1) whether to make plots, or (2) whether to save plots, and if so, in what directory, along with (3) model hyperparameter customization. (3) is currently called "hyperparameters", and is a dict of alpha's (1,2,3), gamma priors (a_k, b_k), and tau. There are internal defaults set within the hierarchical_em_wrapper: all alpha's are currently set to 2; "tau" is currently set to 1, and "tau_grid" is set to [0.1, 0.5, 1.0, 2.0]. Model parameter, "lambda1", is currently set to 0.1, "lambda2" is currently set to 0.5, and "lambda3" is currently set to the empirical mean of the data for a given bait. Why we switched "lambda1" and "lambda3", although I know it relates to "gamma1" and "gamma3", but I don't know how. All three pi initial values are set to floats of 1/3, though I thought we intended to keep these weights at 0.34 for the signal component and half of the remainder for the other two components.

I never see a tau update in the hierarchical_em_wrapper (this is good, because tau isn't updating, we're searching for the best tau fitting the data), so I don't know why we have both a "tau", and a "tau_history" in the dictionary return object.

I'm similarly struggling to connect pipeline output objects and diagnostics input objects.

So it seems we need to clarify the hierarchical pipeline, in particular, the pipeline file, the diagnostics plots, and the tau grid file and tau grid diagnostic plot